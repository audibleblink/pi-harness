{
  "nextId": 6,
  "tasks": [
    {
      "id": "3",
      "subject": "Phase 3 — subagent tools in agents/",
      "description": "Implement Phase 3 of plan.md exactly as specified. Read prd.md and plan.md \"Phase 3\" section first. Phases 1 (a3109ff) and 2 (ed3644c) are done.\n\nDeliverables:\n1. `extensions/agents/subagent-tools.ts` — port `Agent`, `get_subagent_result`, `steer_subagent` registrations + Typebox schemas verbatim from current `extensions/orchestration/index.ts`. Trim to only what the tools need.\n2. `extensions/agents/subagent-runner.ts` — wraps existing `extensions/orchestration/agent-runner.ts` via direct import (`../orchestration/agent-runner.js`). EXPLICITLY ALLOWLISTED transitional cross-extension import; removed in P7. Provide a `spawn(...)`-style helper used by the tool registrations.\n3. `extensions/agents/subagent-bus.ts` — calls `publishOrchestration` and `publishSubagentUsage` from `extensions/ui/bus.ts` at the same publish points orchestration/ uses today.\n4. `extensions/agents/lifecycle-events.ts` — exports event name + payload type. Event name MUST be `agents:subagent_end`. Payload: `{ agentId: string; status: \"success\" | \"failure\" | \"stopped\"; createdByTaskId?: string }`. Document at top of file.\n5. Emit `agents:subagent_end` from runner completion path (success, failure, stop) inside `subagent-runner.ts`'s wrapper.\n6. `extensions/orchestration/index.ts`: register the three subagent tools ONLY when `!settings.agents?.enabled`. Guard must precede registration within ~30 lines.\n7. `extensions/agents/index.ts` registers the three subagent tools (gated on `agents.enabled`).\n8. Update `scripts/check-invariants.sh` allowed-events list to include `agents:subagent_end`.\n\nCreate `scripts/phase3-verify.sh` per plan: 5 checks listed in Phase 3 section. Smoke step: minimal arm or manual-checklist exit 0 (same pattern as Phase 2).\n\nAcceptance: check-build, check-invariants, phase3-verify all exit 0. No TUI chrome in extensions/agents/.\n\nAfter green: update plan.md Phase 3 checkboxes `- [ ]` → `- [x]`. Commit: `phase 3: subagent tools + lifecycle event in agents/`.\n\nReport: files created, verify output, commit SHA. Do NOT proceed to Phase 4.",
      "status": "in_progress",
      "metadata": {
        "agentType": "general-purpose",
        "agentId": "3ede18cb-8cbf-446"
      },
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778268001873,
      "updatedAt": 1778268004020,
      "owner": "3ede18cb-8cbf-446"
    },
    {
      "id": "4",
      "subject": "Phase 6 — cascade event wiring",
      "description": "Implement Phase 6 of plan.md exactly. Phases 1-5 done (a3109ff, ed3644c, 9b38b45, 3a280f1, ac664ce). Read prd.md + plan.md \"Phase 6\" section.\n\nDeliverables:\n1. Replace `extensions/tasks/cascade-stub.ts` with `extensions/tasks/cascade.ts`:\n   - Subscribes to `agents:subagent_end`.\n   - On event, looks up task by `agentId` via TaskStore's agentTaskMap (`getTaskByAgent` helper). Ensure `TaskExecute` populates the binding when spawning.\n   - If found and event status is terminal (success/failure/stopped), mark task completed and trigger same cascade rules as orchestration today (release tasks blocked by this one).\n2. Remove cascade subscription from `orchestration/index.ts` when `tasks.enabled` is true. Explicitly guard against double-fire even if its tools are skipped.\n3. agentTaskMap remains in exactly one file (`extensions/tasks/store.ts`). The existing invariants script must still pass.\n4. NO cross-extension imports between `agents/` and `tasks/` — wiring is purely via the event bus.\n\n`scripts/phase6-verify.sh` per plan:\n1. `rg -l \"agentTaskMap\" extensions/` returns exactly 1 path (extensions/tasks/store.ts).\n2. `rg -n \"agents:subagent_end\" extensions/tasks/cascade.ts` ≥ 1.\n3. `rg -n \"cascade-stub\" extensions/` returns 0.\n4. Smoke: minimal arm or documented manual checklist exiting 0.\n5. check-build + check-invariants exit 0.\n\nAcceptance: all three scripts exit 0.\n\nAfter green: flip plan.md Phase 6 checkboxes. Commit: `phase 6: cascade event wiring (agents → tasks)`.\n\nReport: files, verify output, SHA. Do NOT proceed to Phase 7.",
      "status": "in_progress",
      "metadata": {
        "agentType": "general-purpose",
        "agentId": "c1fe0e72-de79-4c2"
      },
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778269448532,
      "updatedAt": 1778269450539,
      "owner": "c1fe0e72-de79-4c2"
    },
    {
      "id": "5",
      "subject": "Phase 8 — full smoke + acceptance",
      "description": "Implement Phase 8 of plan.md exactly. Phases 1-7 done (a3109ff, ed3644c, 9b38b45, 3a280f1, ac664ce, fe61adf, 72190a9). Read prd.md \"Verification\" section + plan.md \"Phase 8\" section.\n\nDeliverables:\n1. Extend `scripts/smoke.sh` with an `acceptance` case-arm that runs the 12 PRD smoke scenarios (listed verbatim in plan.md Phase 8). For each scenario, prefer a real automated check; if pi's scripted-input mechanism cannot drive a particular scenario non-interactively, fall back to a documented manual checklist line that prints clearly and contributes to a final summary. Each scenario should print PASS / SKIP(manual) / FAIL with rationale.\n\n2. `scripts/phase8-verify.sh` — wraps `scripts/smoke.sh acceptance` and asserts exit 0; also re-runs `bash scripts/check-build.sh` and `bash scripts/check-invariants.sh`.\n\n3. Update PR description / changelog note in this repo (e.g., append to AGENTS.md or a CHANGELOG.md if one exists, otherwise create a brief `CHANGELOG.md` with the migration note about: `defaultAgent` → `defaultPrimaryAgent` rename and the `mode:` default flip from absent→`subagent`-effective to absent→`all`).\n\nAcceptance:\n- `bash scripts/phase8-verify.sh` exits 0.\n- `bash scripts/check-build.sh` and `bash scripts/check-invariants.sh` exit 0.\n\nAfter green: flip plan.md Phase 8 checkboxes `- [ ]` → `- [x]`. Commit: `phase 8: full smoke + acceptance`.\n\nReport: scenarios automated vs manual, verify output, SHA. This is the final phase — migration complete.",
      "status": "in_progress",
      "metadata": {
        "agentType": "general-purpose",
        "agentId": "b3f2685e-d084-496"
      },
      "blocks": [],
      "blockedBy": [],
      "createdAt": 1778270225858,
      "updatedAt": 1778270227980,
      "owner": "b3f2685e-d084-496"
    }
  ]
}